---
# Source: docker-registry/templates/secret.yaml
apiVersion: v1
kind: Secret
metadata:
  name: my-release-docker-registry-secret
  namespace: docker-registry-2.2.3.tgz
  labels:
    app: docker-registry
    chart: docker-registry-2.2.3
    heritage: Helm
    release: my-release
type: Opaque
data:
  haSharedSecret: "QnZTcVRCMVZ1cDBNeFZqbQ=="
  proxyUsername: ""
  proxyPassword: ""
---
# Source: docker-registry/templates/configmap.yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: my-release-docker-registry-config
  namespace: docker-registry-2.2.3.tgz
  labels:
    app: docker-registry
    chart: docker-registry-2.2.3
    heritage: Helm
    release: my-release
data:
  config.yml: |-
    health:
      storagedriver:
        enabled: true
        interval: 10s
        threshold: 3
    http:
      addr: :5000
      debug:
        addr: :5001
        prometheus:
          enabled: false
          path: /metrics
      headers:
        X-Content-Type-Options:
        - nosniff
    log:
      fields:
        service: registry
    storage:
      cache:
        blobdescriptor: inmemory
    version: 0.1
---
# Source: docker-registry/templates/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: my-release-docker-registry
  namespace: docker-registry-2.2.3.tgz
  labels:
    app: docker-registry
    chart: docker-registry-2.2.3
    release: my-release
    heritage: Helm
spec:
  type: ClusterIP
  ports:
    - port: 5000
      protocol: TCP
      name: http-5000
      targetPort: 5000
  selector:
    app: docker-registry
    release: my-release
---
# Source: docker-registry/templates/deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-release-docker-registry
  namespace: docker-registry-2.2.3.tgz
  labels:
    app: docker-registry
    chart: docker-registry-2.2.3
    release: my-release
    heritage: Helm
spec:
  selector:
    matchLabels:
      app: docker-registry
      release: my-release
  replicas: 1
  minReadySeconds: 5
  template:
    metadata:
      labels:
        app: docker-registry
        release: my-release
      annotations:
        checksum/config: 5007f1f825d1dfd543c6e21219c20ae5b3e9421dd7d62247cb6a095ed5f70ce3
        checksum/secret: a015f5dfbd21b555c16d50f0f4c4f4584b2681a639a7a9a33aa6356c530d5a82
    spec:
      securityContext: 
        fsGroup: 1000
        runAsUser: 1000
      containers:
        - name: docker-registry
          image: "registry:2.8.1"
          imagePullPolicy: IfNotPresent
          command:
          - /bin/registry
          - serve
          - /etc/docker/registry/config.yml
          ports:
            - containerPort: 5000
          livenessProbe:
            httpGet:
              path: /
              port: 5000
          readinessProbe:
            httpGet:
              path: /
              port: 5000
          resources: 
            {}
          env: 
            - name: REGISTRY_HTTP_SECRET
              valueFrom:
                secretKeyRef:
                  name: my-release-docker-registry-secret
                  key: haSharedSecret
            - name: REGISTRY_STORAGE_FILESYSTEM_ROOTDIRECTORY
              value: "/var/lib/registry"
          volumeMounts: 
            - name: "my-release-docker-registry-config"
              mountPath: "/etc/docker/registry"
            - name: data
              mountPath: /var/lib/registry/
      volumes: 
        - name: my-release-docker-registry-config
          configMap:
            name: my-release-docker-registry-config
        - name: data
          emptyDir: {}
